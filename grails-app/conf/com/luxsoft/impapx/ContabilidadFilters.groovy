package com.luxsoft.impapx

class ContabilidadFilters {

    def filters = {
		/*
        all(controller:'*', action:'*') {
            before = {

            }
            after = { Map model ->

            }
            afterView = { Exception e ->

            }
        }
        */
		//generarPoliza(controller:)
		
    }
}
